spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3306/kt_company?serverTimezone=Asia/Seoul
    username: root
    password: d3i29wa3
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
      format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
  root: info